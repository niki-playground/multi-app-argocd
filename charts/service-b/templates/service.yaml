apiVersion: v1
kind: Service
metadata:
  name: service-b
  labels:
    app.kubernetes.io/name: service-b
spec:
  selector:
    app.kubernetes.io/name: service-b
  ports:
    - port: 80
      targetPort: {{ .Values.service.port }}
      protocol: TCP
